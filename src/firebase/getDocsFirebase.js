import { getUsers } from './firestore.js';

window.addEventListener('DOMContentLoaded', () => {
    // try {
    getUsers();
    //     return querySnapshot;
    // } catch (error) {
    //     console.log(`error en consulta al pedir user a firestore ${error}`);
    // }
    //}

});
